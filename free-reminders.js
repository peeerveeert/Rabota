(()=>{
const KEY='blocks-reminders-v1';
const load=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch(e){return[]}};
const save=a=>localStorage.setItem(KEY,JSON.stringify(a));
function parse(text,now=new Date()){text=(text||'').trim();let when=null,m,matched='';const tests=[[/(?:через\s+)?полтора\s+часа/i,()=>90],[/(?:через\s+)?пол(?:\s*)часа|через\s+30\s*(?:мин|минут)/i,()=>30],[/через\s+(\d+(?:[.,]\d+)?)\s*(?:час|часа|часов|ч)/i,x=>Math.round(parseFloat(x[1].replace(',','.'))*60)],[/через\s+(\d+)\s*(?:мин|минут|минуты)/i,x=>parseInt(x[1],10)]];for(const [re,mins] of tests){m=text.match(re);if(m){matched=m[0];when=new Date(now.getTime()+mins(m)*60000);break}}if(!when&&(m=text.match(/завтра(?:\s+в)?\s+(\d{1,2})(?::(\d{2}))?/i))){matched=m[0];when=new Date(now);when.setDate(when.getDate()+1);when.setHours(+m[1],+(m[2]||0),0,0)}if(!when&&(m=text.match(/(?:сегодня\s+)?(?:в\s*)?(?:к\s*)?(\d{1,2})[:.](\d{2})/i))){matched=m[0];when=new Date(now);when.setHours(+m[1],+m[2],0,0);if(when<=now)when.setDate(when.getDate()+1)}if(!when)return null;const label=text.replace(matched,'').replace(/^[\s,.:;—-]+|[\s,.:;—-]+$/g,'').trim()||text;return{when:when.getTime(),label}}
function fmt(ts){const d=new Date(ts),n=new Date(),t=new Date();t.setDate(t.getDate()+1);const day=d.toDateString()===n.toDateString()?'Сегодня':d.toDateString()===t.toDateString()?'Завтра':d.toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit'});return `${day}, ${d.toLocaleTimeString('ru-RU',{hour:'2-digit',minute:'2-digit'})}`}
function allFree(){try{return typeof freeNotes!=='undefined'&&Array.isArray(freeNotes)?freeNotes:[]}catch(e){return[]}}
function allBlocks(){try{return typeof blocks!=='undefined'&&Array.isArray(blocks)?blocks:[]}catch(e){return[]}}
function findBlock(text){const low=(text||'').toLowerCase();return allBlocks().filter(b=>b.title&&low.includes(String(b.title).toLowerCase())).sort((a,b)=>String(b.title).length-String(a.title).length)[0]||null}
function syncFreeNotes(showMessage=false){const notes=allFree(),arr=load();let changed=false,created=null;notes.forEach(n=>{const p=parse(n.text),any=arr.find(x=>x.freeNote&&x.noteId===n.id&&!x.done);if(!p||any)return;const b=findBlock(n.text),r={id:'r'+Date.now()+Math.random().toString(36).slice(2,6),noteId:n.id,freeNote:true,done:false,fired:false,createdAt:Date.now(),blockId:b?.id||'FREE',blockTitle:b?.title||'',text:p.label,source:n.text,when:p.when,updatedAt:Date.now()};arr.push(r);changed=true;created=r});if(changed){save(arr);if(showMessage&&created&&typeof showToast==='function')showToast(`🔔 Напоминание создано: ${fmt(created.when)}`)}return changed}
function afterSave(){let tries=0;const refresh=()=>{tries++;const changed=syncFreeNotes(tries===1);if(changed||tries>=8)return;setTimeout(refresh,100)};setTimeout(refresh,0)}
document.addEventListener('click',e=>{const b=e.target.closest&&e.target.closest('button');if(b&&b.closest('#modalBgNote')&&/сохранить/i.test(b.textContent||''))afterSave()},true);
// ВАЖНО: этот модуль больше не рисует и не пересчитывает «Сегодня».
// Единственный владелец dashboard — active-work-ui.js. Иначе два таймера спорили и число прыгало 5↔9.
setInterval(()=>syncFreeNotes(false),1000);syncFreeNotes(false);
})();